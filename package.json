{
  "name": "chas-challenge",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"nodemon server.js --cwd backend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
